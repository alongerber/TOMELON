{
  "name": "tomelon",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "echo 'Development: Edit index.html directly, then run npm run build before deploy'"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0"
  }
}
